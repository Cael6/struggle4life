set_o_button(
    "Battle Room",
    BUTTON_WIDTH,
    BUTTON_HEIGHT,
    BUTTON_TEXT_COLOR,
    goto_r_game,
    so_menu_click
);

disabled_message = "Can't go to battle room without causing crash here.";
enabled = false;
