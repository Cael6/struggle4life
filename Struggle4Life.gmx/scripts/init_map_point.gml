//Variables for o_map_point objects.

connected_points = ds_list_create();
width = -1;
height = -1;
action = -1;
enabled = false;
hover = false;

//Used in a recursive function to determine if points are all connected.
connected = false;