alpha = 0;
steps = 0;
angle = point_direction(x, y, mouse_x, mouse_y);

//Whether the steps are going up or down.
direction_of_steps = 1;

//Number of steps before the flash starts to disappear
max_steps = 2;